apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: appointment-capacity-chatbot-pvc
  namespace: default  # Change to your namespace if needed
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi  # Adjust based on your needs
  # Uncomment and configure if you have a specific storage class
  # storageClassName: standard

